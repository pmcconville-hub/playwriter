---
'playwriter': patch
---

Redact URL credentials, credential-like query parameters, structured network fields, and all form fill values from action recordings. Response bodies with unsafe bounds are omitted instead of being loaded into memory.
