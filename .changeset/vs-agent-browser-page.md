---
'playwriter': patch
---

Add a "vs agent-browser" comparison page and refresh the existing comparison docs. All comparison tables now list Playwriter as the first column. The new page explains why one programmable `execute` tool in your real Chrome beats a fixed command-per-action CLI (reusable `.js` modules that get the live `page`, skill recorder, native `chrome.tabCapture` video, built-in cloud browsers, one-click remote tab sharing). The Playwright MCP, Playwright CLI, and Chrome CDP pages were updated to describe current default behavior accurately (persistent managed profiles, `--extension` attach mode, and Chrome 136's default-profile remote-debugging restriction).
