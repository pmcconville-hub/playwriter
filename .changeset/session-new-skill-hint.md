---
'playwriter': patch
---

The copied remote-control prompt no longer says to skip new tabs. That rule is in `playwriter skill` instead, next to a short note on how remote control works.

`playwriter session new` now prints this on stderr:

```
run playwriter skill to see how to use playwriter if not run already
```
