---
'playwriter': patch
---

The **Remote ON** dropdown now has **Copy id**, which copies only the tunnel id.

Use that when you already have the connect command and just need the id to paste into `--remote`.
